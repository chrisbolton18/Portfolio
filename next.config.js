/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  assetPrefix: '/Portfolio/',
  basePath: '/Portfolio',
}

module.exports = nextConfig

