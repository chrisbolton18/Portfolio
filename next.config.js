/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  images: {
    unoptimized: true,
  },
  assetPrefix: '/Portfolio/',
  basePath: '/Portfolio',
}

module.exports = nextConfig

